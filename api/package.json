{
  "name": "api",
  "version": "1.0.0",
  "description": "GET - verifies username avalible\r POST - creates a new user with the given info",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "azure-storage": "^2.10.3"
  }
}
